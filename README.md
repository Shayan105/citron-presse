This is a Web Server to allow users to automatically record the PresseCitron from RFJ so that no way to miss it. 

It basically opens a selenium browser on time and record the audio. Then the audio goes through a transcriber model and then the transcript goes to Gemini to identify the topic of the quizz. 

The Website is written in plain JS, HTML and CSS. 
